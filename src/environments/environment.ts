// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
     // Your web app's Firebase configuration
     firebaseConfig: {
      apiKey: "AIzaSyA7fY_Rw9peUTNE0WrERR6NpCM-ZkCSIgQ",
      authDomain: "test-app-95ba5.firebaseapp.com",
      databaseURL: "https://test-app-95ba5.firebaseio.com",
      projectId: "test-app-95ba5",
      storageBucket: "test-app-95ba5.appspot.com",
      messagingSenderId: "504444537905",
      appId: "1:504444537905:web:48f7a02623ce7fdc"
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
